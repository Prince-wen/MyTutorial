package tutorial.Scala4_FunctionalProgramming.summer

import java.net.{ServerSocket, Socket}

/**
 * @author Shiwen Li
 * @account CN116237626
 * @create 2021-03-25 9:31
 * @note
 */
object Server extends Application {
  def main(args: Array[String]): Unit = {

/*    start("serverSocket"){
      val server: ServerSocket = envData.asInstanceOf[ServerSocket]
      while (true){
        val client:Socket = server.accept()
        new Thread(
          new Runnable {
            override def run(): Unit = {

            }
          }
        ).start()
      }

    }*/

  }
}
